interface RoleDataDTO {
  description: string;
}

const RoleData: RoleDataDTO[] = [
  { description: 'Aluno' },
  { description: 'Professor' },
  { description: 'Funcionário' },
];

export default RoleData;
